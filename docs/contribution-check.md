# GitHub Contribution Check

This repository counts commits on the GitHub contribution graph when:

- commits are authored with a verified GitHub email;
- changes are pushed to the default branch, `main`;
- commits are associated with the `owlandauk` GitHub account.

Current repository identity:

- author: `owlandauk <1242756510@qq.com>`
- default branch: `main`

